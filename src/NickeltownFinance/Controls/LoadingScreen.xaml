<UserControl x:Class="NickeltownFinance.Controls.LoadingScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:NickeltownFinance.Controls"
             x:Name="Root"
             Visibility="{Binding IsLoading, ElementName=Root, Converter={StaticResource BoolToVisibilityConverter}}">
    <Border Background="{StaticResource BrandOverlayBrush}">
        <controls:RaceTrackProgress HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="440"
                                    MaxWidth="90%"
                                    Margin="24"
                                    IsIndeterminate="True"
                                    StageLabel="{Binding Message, ElementName=Root}"
                                    ProgressLabel="…"
                                    DetailText="Hang tight — still rolling." />
    </Border>
</UserControl>
